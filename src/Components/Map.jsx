import React from "react";
import { styles } from "../style";

const Map = () => {
  return (
    <section className={`${styles.marginT}`}>
      <img
        src="./images/Maps.png"
        className="w-full h-[729px] object-cover"
        alt=""
      />
    </section>
  );
};

export default Map;
